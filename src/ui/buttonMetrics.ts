/**
 * Single source of truth for all button-like controls (Button, ChoiceChip,
 * modal primary actions, etc.) so they stay visually consistent.
 */
export const BUTTON_BORDER_RADIUS = 16;
export const BUTTON_PADDING_VERTICAL = 14;
export const BUTTON_PADDING_HORIZONTAL = 16;
export const BUTTON_FONT_SIZE = 16;
export const BUTTON_FONT_WEIGHT = "600" as const;
